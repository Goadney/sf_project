{{ form_start(form, {'attr' : {'class' :'card p-3 w-75 mx-auto' }}) }}

{{ form_widget(form)}}
<div class="text-center">
	<button class="submit btn btn-secondary">{{button_label|default('save')}}</button>
</div>
{# il generer tout le formulaire #}
{{form_end(form)}}
